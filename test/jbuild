;; This is free and unencumbered software released into the public domain.

(jbuild_version 1)

(executable
 ((name test)
  (libraries (cli dry))))

(alias
 ((name runtest)
  (deps (test.exe))
  (action (run ${<}))))
