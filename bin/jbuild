;; This is free and unencumbered software released into the public domain.

(jbuild_version 1)

(executables
 ((names (analyze describe export index locate parse shell))
  ;(public_names (dry-analyze dry-describe dry-export dry-index dry-locate dry-parse dry-shell))
  (libraries (DRY Drylang cmdliner))))
