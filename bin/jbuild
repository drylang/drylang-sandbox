;; This is free and unencumbered software released into the public domain.

(jbuild_version 1)

(executables
 ((names (describe export shell))
  ;(public_names (dry-describe dry-export dry-shell))
  (libraries (cli dry cmdliner))))
